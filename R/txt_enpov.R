txts$enpov$shortitle <- "Energy vulnerability"
txts$enpov$title <- "Analyzing the spatially varying dimensions of energy vulnerability across the EU"
txts$enpov$icon <- icon("plug-circle-exclamation")
txts$enpov$tags <- c(
  "poverty", "vulnerability", "index", "multivariate", "principal", "component",
  "gwpca", "pca"
)
txts$enpov$authors <- c("Jonas Lieth", "Dennis Abel", "Stefan J\u00fcnger")
txts$enpov$affil <- "GESIS - Leibniz Institute for the Social Sciences"
txts$enpov$date <- "2023-mm-dd"
